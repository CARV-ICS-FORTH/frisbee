## @section Cockroach Configuration parameters

## @param port  CockroachDB's port to listen to inter-communications and client connections.
## @param httpPort  CockroachDB's port to listen to HTTP requests.

port: 26257
httpPort: 8080
