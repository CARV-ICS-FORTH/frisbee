---
apiVersion: frisbee.dev/v1alpha1
kind: Scenario
metadata:
  name: hello-world
spec:
  actions:
    - action: Service
      name: hello-world
      service:
        templateRef: hello-world
        inputs:
          - { msg: "hello world" }
