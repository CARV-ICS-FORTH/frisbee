---
apiVersion: frisbee.dev/v1alpha1
kind: Scenario
metadata:
  name: hello-world
spec:
  actions:
    - action: Service
      name: whalesay
      service:
        templateRef: whalesay
        inputs:
          - { message: "hello world", sleep: "10m" }
