---
apiVersion: frisbee.dev/v1alpha1
kind: TestPlan
metadata:
  name: formatted-device
spec:
  actions:
    - action: Service
      name: fio
      service:
        templateRef: fio.benchmark.formatted-device
        inputs:
          - { direct: "0" }