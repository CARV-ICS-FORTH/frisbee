chaos-mesh:
  enabled: false


# OpenEBS - Provision dynamic volumes
openebs:
  enabled: true
  ndm:
    enabled: false
  ndmOperator:
    enabled: false
  defaultStorageConfig:
    enabled: false


# Provision the infrastructure manually
manual:
  enabled: false

# Observability
observability:


# Global values are values that can be accessed from any chart or subchart by exactly the same name. 
# They should be accessed explicitly via {{.Values.global.salad}}
global:
  salad: caesar
