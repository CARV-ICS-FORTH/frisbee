---
apiVersion: frisbee.io/v1alpha1
kind: Template
metadata:
  name: paper.partition.fault

spec:
  inputs:
    parameters:
      target: localhost
      duration: "2m"
  chaos:
    type: partition
    partition:
      service: {{"{{.Inputs.Parameters.target}}"}}
      duration: {{"{{.Inputs.Parameters.duration}}"}}
