# This experiment is designed for the evaluation of various ML frameworks on the client.
# For this purpose, we use a single client, and we change its backend.
---
apiVersion: frisbee.dev/v1alpha1
kind: Scenario
metadata:
  name: ml-backend
spec:
  actions:
    # Step 1: Create FedBed server
    - action: Service
      name: server
      service:
        templateRef: fedbed.server
        inputs:
          - { min_fit_clients: 1, min_available_clients: 1 }

    # Step 2: Create FedBed clients
    - action: Cluster
      name: clients
      depends: { running: [ server ]}
      cluster:
        templateRef: fedbed.client
        inputs:
          - { fl_server: server, dataset: "MNIST", backend: "mxnet", total_nodes: 1, node_id: 0 }


    # Teardown
    - action: Delete
      name: teardown
      depends: { success: [ clients ] }
      delete:
        jobs: [ server ]
