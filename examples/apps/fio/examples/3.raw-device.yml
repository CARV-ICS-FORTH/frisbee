---
apiVersion: frisbee.dev/v1alpha1
kind: Scenario
metadata:
  name: raw-device
spec:
  actions:
    - action: Service
      name: fio
      service:
        templateRef: fio.benchmark.raw-device
        inputs:
          - { direct: "0" }


    # Teardown
    - action: Delete
      name: teardown
      depends: { success: [ fio ] }
      delete:
        jobs: [ ]